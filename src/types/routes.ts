export enum Pages {
  Home = "/",
  Test = "/test",
  TableView = "/table-view",
  BarChart = "/bar-chart",
  StackedBarChart = "/stacked-bar-chart",
  Settings = "/settings",
  FileOpener = "/file-opener",
}
