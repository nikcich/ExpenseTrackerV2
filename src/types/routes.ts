export enum Pages {
  Overview = "/",
  Accounts = "/accounts",
  RSU = "/rsu",
  TableView = "/table-view",
  Charts = "/charts",
  Forecast = "/forecast",
  Data = "/data",
  SSDI = "/ssdi",
  SelectionInsights = "/selection-insights",
  Groups = "/groups",
  Anomalies = "/anomalies",
}
